# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login-e2e.spec.js >> Partner 로그인 (localhost:3200) >> 정상 로그인 → 대시보드 진입, CORS 에러 없음
- Location: scripts/login-e2e.spec.js:53:3

# Error details

```
Error: 로그인 후 여전히 /login 페이지
```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - heading "POCHAK 파트너" [level=1] [ref=e6]
    - paragraph [ref=e7]: 파트너 계정으로 로그인하세요
  - generic [ref=e8]:
    - generic [ref=e9]:
      - generic [ref=e10]: 이메일
      - textbox "partner@example.com" [ref=e11]: partner@pochak.live
    - generic [ref=e12]:
      - generic [ref=e13]: 비밀번호
      - textbox "••••••••" [ref=e14]: partner1234!
    - paragraph [ref=e15]: 이메일 또는 비밀번호가 올바르지 않습니다.
    - button "로그인" [ref=e16]
```

# Test source

```ts
  1   | // @ts-check
  2   | const { test, expect } = require('@playwright/test');
  3   | 
  4   | // ============================================================
  5   | // BO (localhost:3000) - Next.js SSR admin back-office
  6   | // ============================================================
  7   | test.describe('BO 로그인 (localhost:3000)', () => {
  8   |   test('정상 로그인 → 대시보드 진입, API 401 없음', async ({ page }) => {
  9   |     const api401Errors = [];
  10  |     page.on('response', (response) => {
  11  |       if (response.url().includes('/api/') && response.status() === 401) {
  12  |         api401Errors.push(response.url());
  13  |       }
  14  |     });
  15  | 
  16  |     await page.goto('http://localhost:3000/login');
  17  |     await expect(page).toHaveTitle(/POCHAK/i);
  18  | 
  19  |     await page.fill('#username', 'admin');
  20  |     await page.fill('#password', 'admin1234!');
  21  |     await page.click('button[type="submit"]');
  22  | 
  23  |     // 대시보드로 이동 확인
  24  |     await page.waitForURL(/\/(dashboard|$)/, { timeout: 10000 });
  25  |     const url = page.url();
  26  |     expect(url).not.toContain('/login');
  27  | 
  28  |     // API 401 없음 확인
  29  |     expect(api401Errors).toHaveLength(0);
  30  |   });
  31  | 
  32  |   test('잘못된 비밀번호 → 에러 메시지 표시', async ({ page }) => {
  33  |     await page.goto('http://localhost:3000/login');
  34  | 
  35  |     await page.fill('#username', 'admin');
  36  |     await page.fill('#password', 'wrongpassword!');
  37  |     await page.click('button[type="submit"]');
  38  | 
  39  |     // 에러 메시지 노출 (로그인 페이지에 머무름)
  40  |     await page.waitForTimeout(2000);
  41  |     const stillOnLogin = page.url().includes('/login') || page.url() === 'http://localhost:3000/';
  42  |     const errorVisible = await page.locator('text=/잘못|틀린|올바르지|실패|error|invalid|incorrect/i').first().isVisible().catch(() => false);
  43  | 
  44  |     // 로그인 페이지에 머무르거나 에러 표시되어야 함
  45  |     expect(stillOnLogin || errorVisible).toBe(true);
  46  |   });
  47  | });
  48  | 
  49  | // ============================================================
  50  | // Partner (localhost:3200) - React SPA partner portal
  51  | // ============================================================
  52  | test.describe('Partner 로그인 (localhost:3200)', () => {
  53  |   test('정상 로그인 → 대시보드 진입, CORS 에러 없음', async ({ page }) => {
  54  |     const corsErrors = [];
  55  |     page.on('console', (msg) => {
  56  |       if (msg.type() === 'error' && msg.text().toLowerCase().includes('cors')) {
  57  |         corsErrors.push(msg.text());
  58  |       }
  59  |     });
  60  |     page.on('requestfailed', (req) => {
  61  |       const failure = req.failure();
  62  |       if (failure && failure.errorText.toLowerCase().includes('cors')) {
  63  |         corsErrors.push(`CORS: ${req.url()}`);
  64  |       }
  65  |     });
  66  | 
  67  |     await page.goto('http://localhost:3200/login');
  68  | 
  69  |     // 이메일 / 비밀번호 입력 (type=email, type=password)
  70  |     await page.locator('input[type="email"]').fill('partner@pochak.live');
  71  |     await page.locator('input[type="password"]').fill('partner1234!');
  72  |     await page.locator('button[type="submit"]').click();
  73  | 
  74  |     // 대시보드로 이동 확인
  75  |     await page.waitForURL('**/dashboard**', { timeout: 10000 }).catch(async () => {
  76  |       // fallback: 단순히 login이 아닌 URL이면 통과
  77  |       const url = page.url();
> 78  |       if (url.includes('/login')) throw new Error('로그인 후 여전히 /login 페이지');
      |                                         ^ Error: 로그인 후 여전히 /login 페이지
  79  |     });
  80  | 
  81  |     // CORS 에러 없음
  82  |     expect(corsErrors).toHaveLength(0);
  83  |   });
  84  | });
  85  | 
  86  | // ============================================================
  87  | // Web (localhost:3300) - React SPA public web
  88  | // ============================================================
  89  | test.describe('Web 로그인 (localhost:3300)', () => {
  90  |   test('정상 로그인 → 메인 페이지(/home) 진입', async ({ page }) => {
  91  |     await page.goto('http://localhost:3300/login');
  92  | 
  93  |     // 아이디, 비밀번호 입력
  94  |     const emailInput = page.locator('input[type="email"], input[placeholder*="이메일"], input[placeholder*="아이디"]').first();
  95  |     const passwordInput = page.locator('input[type="password"]').first();
  96  | 
  97  |     await emailInput.fill('test@pochak.live');
  98  |     await passwordInput.fill('test1234!');
  99  | 
  100 |     // 로그인 버튼 클릭
  101 |     await page.locator('button[type="submit"]').click();
  102 | 
  103 |     // /home 또는 로그인이 아닌 페이지로 이동 확인
  104 |     await page.waitForURL(/\/(home|$)/, { timeout: 10000 }).catch(async () => {
  105 |       const url = page.url();
  106 |       if (url.includes('/login')) throw new Error('로그인 후 여전히 /login 페이지');
  107 |     });
  108 | 
  109 |     const finalUrl = page.url();
  110 |     expect(finalUrl).not.toContain('/login');
  111 |   });
  112 | 
  113 |   test('미인증 상태에서 /my 접근 시 /login으로 리다이렉트', async ({ page }) => {
  114 |     // localStorage 초기화 (비인증 상태)
  115 |     await page.goto('http://localhost:3300');
  116 |     await page.evaluate(() => {
  117 |       localStorage.removeItem('pochak_token');
  118 |       localStorage.removeItem('pochak_user');
  119 |     });
  120 | 
  121 |     // /my 직접 접근
  122 |     await page.goto('http://localhost:3300/my');
  123 | 
  124 |     // /login으로 리다이렉트 확인
  125 |     await page.waitForURL('**/login**', { timeout: 5000 });
  126 |     expect(page.url()).toContain('/login');
  127 |   });
  128 | });
  129 | 
```